# Storage indexes. Unset these to run without databases — the archive on disk
# remains the source of truth either way (docs/22).
POSTGRES_URL=Host=postgres;Port=5432;Username=apex;Password=apex;Database=apex
INFLUX_URL=http://influxdb:8086
INFLUX_TOKEN=apex-dev-token
INFLUX_ORG=apex
INFLUX_BUCKET=telemetry
MONGO_URL=mongodb://apex:apex@mongo:27017/?authSource=admin
